apiVersion: v1
kind: Service
metadata:
  name: otp-redis
  namespace: otp-relay
  labels:
    app: otp-redis
spec:
  type: ClusterIP
  selector:
    app: otp-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
      protocol: TCP
