apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: otp-relay-data
  namespace: otp-relay
  labels:
    app: otp-relay
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
